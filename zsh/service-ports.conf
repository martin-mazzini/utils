#!/bin/bash
# Service Port Configuration File
# This file contains the REFERENCE port configuration for all services
# These are the default/standard ports - this file should NOT be modified
# Format: SERVICE_PORTS[service-name]="type:port,type:port,..."

declare -A SERVICE_PORTS

# Example App Service
SERVICE_PORTS[my-app-service]="ui:3000,server:8000,postgres:5432"
